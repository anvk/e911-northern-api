/* global it, describe, require */

var chai = require('chai'),
    expect = chai.expect,
    googleStocks = require('../dist/northern911-api.js');

describe('northern911-api tests', function() {

  it('dumb test', function() {
    expect(true).to.be.true;
  });

});
